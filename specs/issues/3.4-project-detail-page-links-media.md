## **Status:**
- Review: Approved
- PR: Todo

## Metadata
- **Title:** Project detail page - Links & media
- **Phase:** Phase 3: Projects Feature
- **GitHub Issue:**

---

## Description
Enhance the project detail page by adding functional links for GitHub and Live Demo, displaying the project thumbnail with high quality, and implementing image lazy-loading for better performance. This task also includes preparing the social share section.

---

## Acceptance Criteria
- [ ] GitHub link is displayed with an icon and opens in a new tab.
- [ ] Live Demo link is displayed with an icon and opens in a new tab.
- [ ] Project thumbnail is displayed prominently and responsively.
- [ ] Images (thumbnail and content images) use native lazy-loading (`loading="lazy"`).
- [ ] Social share section is present (placeholder or icons prepared for Phase 7).
- [ ] Links only appear if the corresponding URL exists in the database.

---

## Implementation Checklist
- [ ] Update `src/pages/ProjectDetail.vue` to include:
    - [ ] Dynamic links section for GitHub and Live Demo.
    - [ ] Responsive thumbnail container with optimized image attributes.
    - [ ] Logic to conditionally render links only if they are not null.
    - [ ] Preparation for social share buttons.
- [ ] Add icons for GitHub, External Link, and Social platforms (consider using simple SVG icons).
- [ ] Verify image lazy-loading works in the browser.

---

## Step-by-step Guide

**Files to create/modify:**
- `src/pages/ProjectDetail.vue` — Add links, media, and sharing UI.

**Key decisions:**
- Use native `loading="lazy"` for simplicity and performance.
- Use conditional rendering (`v-if`) for external links to handle missing data gracefully.
- Keep the share section simple for now (Phase 7 will add full functionality).

**Non-obvious snippets**:
```html
<a v-if="project.github_url" :href="project.github_url" target="_blank" rel="noopener noreferrer">
  <GithubIcon class="w-5 h-5" />
  <span>GitHub</span>
</a>
```

---

## Notes
- Dependency on Task 3.3 (Basic project detail page structure).
- Ensure `rel="noopener noreferrer"` is used for all external links for security.
- Icons should follow the project's minimal aesthetic.
