## **Status:**
- Review: Approved
- PR: Draft

## Metadata
- **Title:** Home page - Tech stack showcase
- **Phase:** 5 Home Page
- **GitHub Issue:** #38

---

## Description
Create a dedicated "Tech Stack" section on the Home page to visually showcase the core technologies and tools used. This adds professional credibility and allows visitors to quickly see the developer's expertise.

---

## Acceptance Criteria
- [x] Create a `TechStack.vue` component in `src/components/`.
- [x] Display a list of technologies: Vue.js, React, Node.js, PostgreSQL, Tailwind CSS, TypeScript, Supabase.
- [x] Implement a clean grid or horizontally scrolling marquee layout.
- [x] Add hover effects (e.g., scale-up or glow) for each technology item.
- [x] Ensure the section is fully responsive on mobile (stacking or smaller icons).
- [x] Use `useScrollReveal` to animate the section as it enters the viewport.
- [x] Integrate the section into `src/pages/Home.vue` between the Hero and Featured Projects sections.

---

## Implementation Checklist
- [x] Prepare SVG icons for the selected technologies.
- [x] Create `src/components/TechStack.vue`.
- [x] Map through a list of technologies to render icons and labels.
- [x] Style the section with Tailwind CSS for a modern, minimalist look.
- [x] Import and add `<TechStack />` to `src/pages/Home.vue`.
- [x] Verify responsiveness and animation smoothness.

---

## Step-by-step Guide

**Files to create/modify:**
- `src/components/TechStack.vue` — New component for showcasing technologies.
- `src/pages/Home.vue` — Add the new section between the Hero and Featured Projects.

**Key decisions:**
- Use a simple CSS Grid with `gap-8` for the desktop view, transitioning to a smaller grid on mobile.
- Use `useScrollReveal` for a standard entry animation (fade and slide up).

**Non-obvious snippets** *(only if needed)*:
```vue
<!-- Inside TechStack.vue -->
<script setup lang="ts">
import { useScrollReveal } from '../composables/useScrollReveal'
const { targetRef, isVisible } = useScrollReveal()
const techs = [
  { name: 'Vue.js', icon: '...' },
  // ...
]
</script>
```

---

## Notes
- Keep icons consistent in size and color scheme (e.g., grayscale with color on hover).
- Ensure high contrast for accessibility.
