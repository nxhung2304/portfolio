## **Status:**
- Review: Approved
- PR: Draft

## Metadata
- **Title:** Blog list - Fetching & pagination
- **Phase:** Phase 4: Blog Feature
- **GitHub Issue:** (to be filled after sync)

---

## Description
Create the main blog list page that fetches and displays all published posts from Supabase. This task includes setting up the basic UI for the blog listing and implementing pagination to handle large amounts of posts efficiently.

---

## Acceptance Criteria
- [x] Accessible at `/blog` route.
- [x] Only posts with `published=true` are visible.
- [x] Each post card shows: title, excerpt, publish date (formatted), and tech/topic tags.
- [x] Pagination or infinite scroll works as expected.
- [x] Loading states are shown during data fetching.
- [x] Empty state shown if no posts exist.

---

## Implementation Checklist
- [x] Create `src/pages/Blog.vue` (rename or update existing if any).
- [x] Implement Supabase query to fetch posts from `posts` table.
- [x] Add pagination logic (limit, offset) or a "Load More" button.
- [x] Create a reusable `BlogCard` component (optional, can be inline first).
- [x] Add `v-if` loading skeleton or spinner.
- [x] Update `src/router/index.ts` if route path needs adjustment.

---

## Step-by-step Guide

**Files to create/modify:**
- `src/pages/Blog.vue` — Main blog listing page.
- `src/router/index.ts` — Ensure `/blog` route points to `Blog.vue`.
- `src/lib/supabase.ts` — Use existing client to query the `posts` table.

**Key decisions:**
- **Pagination Strategy:** Use "Load More" button for simplicity and better mobile UX.
- **Sorting:** Default sorting should be `published_at` descending.
- **Data Fetching:** Use a `pageSize` of 6 or 9 posts per page.

**Non-obvious snippets**:
```ts
const { data, count, error } = await supabase
  .from('posts')
  .select('*', { count: 'exact' })
  .eq('published', true)
  .order('published_at', { ascending: false })
  .range(from, to);
```

---

## Notes
- Ensure `published_at` is used for the date display, falling back to `created_at` if null.
- Tags in the `posts` table are stored as a JSONB array; ensure proper typing in Vue.
