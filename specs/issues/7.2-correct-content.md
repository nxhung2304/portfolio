## **Status:**
- Review: Approved
- PR: Draft

## Metadata
- **Title:** Correct content on About page (timeline & skills)
- **Phase:** 7 – Refinements
- **GitHub Issue:** #50

---

## Description

Replace all placeholder/mock content on the About page with real data:
1. **Timeline** – rewrite `timelineItems` to reflect actual work history (2021–2026).
2. **Professional Skills** – rewrite the `SKILLS` constant to match the skills listed in `public/cv-en.html` / `public/cv-vi.html`.

---

## Acceptance Criteria
- [ ] Timeline shows correct years, roles, and descriptions matching the real work history
- [ ] Timeline is bilingual (en/vi) via the existing i18n approach (`locale.value === 'vi'`)
- [ ] Skills section categories and items match what's in the CV HTML files
- [ ] No placeholder company names ("Tech Corp", "Startup XYZ", "Agency ABC") remain
- [ ] Stats block values remain reasonable (update if needed)
- [ ] Page renders without errors in both EN and VI

---

## Implementation Checklist
- [ ] Update `timelineItems` in `src/pages/About.vue` with real history:
  - 2021–2023: Cordova + Vue.js (mobile apps: golf, camp, children); Vue.js + Rails web (Microsoft 365 mailboxes)
  - 2024: Rails, Flutter, iOS native (adapt new API)
  - 2025: Flutter BLE, Rails API; Microsoft (OAuth, Classroom API), Google Cloud (OAuth, Classroom API); iOS native (AppFlyer – cloud setup + Onelink)
  - 2026: Rails API CRUD, Flutter BLE, Microsoft B2C
- [ ] Add bilingual descriptions for each timeline entry
- [ ] Replace `SKILLS` constant with groups and items sourced from `public/cv-en.html`:
  - Mobile, Frontend, Backend, Firebase, Auth & DevOps, AI Tools (match existing groups in the CV)
- [ ] Review stats block — update numbers if they no longer fit

---

## Step-by-step Guide

**Files to modify:**
- `src/pages/About.vue` — `timelineItems` computed (lines ~169–206) and `SKILLS` constant (lines ~208–212)
- `public/cv-en.html` — read-only reference for skills groups
- `public/cv-vi.html` — read-only reference for Vietnamese skill labels (if any)

**Key decisions:**
- Keep the existing `locale.value === 'vi'` inline bilingual pattern — do not introduce new i18n keys for this task
- Timeline `type` field: use `'work'` for all 2021–2026 entries (all are work experience)
- For company name, use the actual company or project context; if confidential, use a generic descriptor like `"Freelance / Outsource"`

**Skills reference** (from `public/cv-en.html` lines ~179–228):
```
Mobile:       Flutter, iOS (Swift/ObjC), Cordova
Frontend:     Vue.js, Nuxt.js, TypeScript, Tailwind CSS
Backend:      Ruby on Rails, REST API
Firebase:     (check cv-en.html for items)
Auth & DevOps: OAuth, Microsoft B2C, Google Cloud, CI/CD
AI Tools:     (check cv-en.html for items)
```

---

## Notes
- Do not touch i18n locale files (`src/locales/en.json`, `vi.json`) — timeline content stays inline in About.vue for now
- `public/cv-en.html` is the single source of truth for skill items; mirror those exactly
- Stats (`5+`, `30+`, `50K+`, `99.9%`) are placeholders — update if there's real data, otherwise leave unchanged
